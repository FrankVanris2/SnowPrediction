import React from 'react';
import {GlobalStyle, HelloWorld} from './App.style';

const App = () => (
  <>
    <GlobalStyle />

    {/* Example component (remove it when writing your own code) */}
    <HelloWorld>
      Hello, world!
    </HelloWorld>
  </>
);

export default App;
